name: Auto Scrape M3U Playlist - Chuoi Chien TV

on:
  schedule:
    # Tự động chạy mỗi 1 tiếng một lần (Cron theo giờ UTC)
    - cron: '0 * * * *'
  workflow_dispatch: # Giữ lại nút bấm thủ công khi muốn test ngay

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install playwright
          playwright install chromium
          playwright install-deps

      - name: Run Scraper Script
        run: |
          python scraper.py

      - name: Commit and Push Playlist
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Auto update playlist.m3u"
          file_pattern: "playlist.m3u"
          
